<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- the theme applied to the application or activity -->
    <style name="ProsperInvestAppTheme"
        parent="@android:style/Theme.Holo">
        <item name="android:actionBarStyle">@style/ProsperInvestActionBar</item>
        <item name="android:buttonStyle">@style/prosper_normal_button</item>
        <item name="android:buttonStyleToggle">@style/prosper_toggle_button</item>
    </style>

    <!-- ActionBar styles -->
    <style name="ProsperInvestActionBar"
        parent="@android:style/Widget.Holo.ActionBar">
        <item name="android:background">@color/prosper_color_darker</item>
        <item name="android:textColor">@color/prosper_white</item>
        <item name="android:textSize">@dimen/prosper_text_size_large</item>

    </style>
</resources>